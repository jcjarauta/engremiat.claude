{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://engremiat.local/telar/recurso.schema.json",
  "title": "Recurso",
  "description": "PROPUESTA_BASTIDOR_GESTOR_PROYECTOS_ENGREMIAT.md §8.12 -- espejo real de dos fuentes reales distintas, honesto sobre que no son la misma: (a) 01_Mundo/Recursos/*.md del vault, hoy solo recursos OPERATIVOS/de gobierno (GASTO_API, 92_BUS_TRABAJO); (b) 23_RECURSO del Sheet real, recursos FISICOS/de negocio (ID/CODIGO/NOMBRE/CLASE_RECURSO/COSTE_ADQUISICION/COSTE_PERIODICO/CAPACIDAD). Son primos, no gemelos -- 'clase' distingue cual es cual, nunca se fuerza una equivalencia que no existe.",
  "type": "object",
  "required": ["id", "nombre", "clase", "estado"],
  "additionalProperties": false,
  "properties": {
    "_fixture": { "type": "boolean", "const": true },
    "id": { "type": "string", "minLength": 1 },
    "nombre": { "type": "string", "minLength": 1, "description": "Pregunta Que." },
    "resumen": { "type": "string", "description": "Pregunta Que, ampliada -- espejo del cuerpo real de la ficha .md." },
    "clase": {
      "type": "string",
      "enum": ["operativo_gobierno", "fisico_negocio"],
      "description": "Pregunta Que -- distingue las dos fuentes reales (ver descripcion del esquema). operativo_gobierno = como GASTO_API/92_BUS_TRABAJO; fisico_negocio = como 23_RECURSO/08_MATERIALES del Sheet."
    },
    "vinculoReal": {
      "type": "array",
      "description": "Pregunta Donde -- mismo patron que espacio.schema.json/mision.schema.json.",
      "items": {
        "type": "object",
        "required": ["sistema", "recordId"],
        "additionalProperties": false,
        "properties": {
          "sistema": { "type": "string", "minLength": 1 },
          "recordId": { "type": "string", "minLength": 1 }
        }
      }
    },
    "costeAdquisicion": { "type": ["number", "null"], "description": "Pregunta Cuanto -- espejo de COSTE_ADQUISICION (23_RECURSO), null si clase=operativo_gobierno." },
    "costePeriodico": { "type": ["number", "null"], "description": "Pregunta Cuanto -- espejo de COSTE_PERIODICO (23_RECURSO)." },
    "responsableId": { "type": ["string", "null"], "description": "Pregunta Quien -- espejo de RESPONSABLE_ID (23_RECURSO)." },
    "estado": { "type": "string", "description": "Pregunta Cuando -- espejo directo de ESTADO real." }
  }
}
