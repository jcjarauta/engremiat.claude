# INC-0008 -- CosteService no debe crashear si el cliente no tiene COMPRAS

## Contexto

`src/CosteService.js`, función `calcularCosteMaterialesEstimado_` (línea 52).
Lee las hojas MATERIAL/PROVEEDOR_MATERIAL/PRODUCTO_MATERIAL (del módulo
COMPRAS) con `listarRegistros` directo -- crashea si el cliente no tiene
COMPRAS instalado. La misma función ya usa `listarRegistrosSeguro_` para
RECURSO (mismo patrón, unas líneas más abajo en `calcularCosteRecursosAtribuido_`)
-- solo hay que aplicar la misma función segura aquí.

## Resultado esperado

En `calcularCosteMaterialesEstimado_`, cambiar EXACTAMENTE estas 3 líneas
(y solo estas, no tocar nada más del fichero):

```
  var lineas = listarRegistros('PRODUCTO_MATERIAL', { ACTIVO: 'SÍ' })
```
->
```
  var lineas = listarRegistrosSeguro_('PRODUCTO_MATERIAL', { ACTIVO: 'SÍ' })
```

```
  listarRegistros('MATERIAL', {}).forEach(function (m) { materialesPorId[m.ID] = m; });
```
->
```
  listarRegistrosSeguro_('MATERIAL', {}).forEach(function (m) { materialesPorId[m.ID] = m; });
```

```
  listarRegistros('PROVEEDOR_MATERIAL', { ACTIVO: 'SÍ' }).forEach(function (pm) {
```
->
```
  listarRegistrosSeguro_('PROVEEDOR_MATERIAL', { ACTIVO: 'SÍ' }).forEach(function (pm) {
```

## Criterios de aceptación

- Solo se tocan esas 3 líneas, en `src/CosteService.js`.
- No se toca `costeDiarioRecurso_`, `calcularCosteRecursosAtribuido_` ni
  ninguna otra función.
- `listarRegistrosSeguro_` ya existe en el proyecto (Repository.js) -- no
  se inventa ni se define de nuevo.

## Definition of done

1. Cambios hechos en `src/CosteService.js`.
2. Mensaje final con el formato ESTADO/RESUMEN/ARCHIVOS/GATES de `.clinerules`.
